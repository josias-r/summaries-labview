= Sampling and Acquisition
#rect()[
  Sampling can be done on the hardware via a Sample Clock node. This will be more accurate and have less _jitter_ then if the sampling is done in software.
]
#rect(inset: 1pt)[
  #image("/assets/image.png")
]
== Sampling Terminology
#rect(inset: 1pt)[
  #image("/assets/image-1.png")
]
#rect()[
  More channels limits the sampling rate! Two channels -> half sample rate
]
